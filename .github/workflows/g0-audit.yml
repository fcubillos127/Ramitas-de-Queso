name: Numerical certification

on:
  push:
    branches:
      - audit/g0-certification
  pull_request:
    branches:
      - claude/code-review-optimization-49vxac

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run numerical certification tests
        run: python -m unittest discover -s tests -p 'test_*.py' -v
      - name: Run focused n_suma convergence audit
        run: >-
          python scripts_figs/audit_nsum_convergence.py
          --k-over-pi 1.5
          --n-values 8 12 20 30 40 60
          --ngrid 120
          --stable-steps 2
          --freq-atol 5e-5
          --freq-rtol 5e-5
          --residual-tol 1e-6
